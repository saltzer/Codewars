Write function makeUpperCase.

----------------------
#include <string>

std::string makeUpperCase (const std::string& input_str){
    std::string res;
    std::transform(input_str.begin(), input_str.end(), std::back_inserter(res), toupper);
    return(res);
}
----------------------
